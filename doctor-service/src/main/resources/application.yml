spring:
    application:
        name: doctor-service

    data:
        mongodb:
            uri: mongodb://root:rootpassword@localhost:27017/healthcare?authSource=admin

logging:
    level:
        org:
            springframework:
                web: DEBUG
                servlet: DEBUG

server:
    port: ${DOCTOR_SERVICE_PORT:8081}

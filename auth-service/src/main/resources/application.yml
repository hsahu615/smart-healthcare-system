spring:
  application:
    name: auth-service

  data:
    mongodb:
      uri: ${MONGO_URI}

logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.servlet: DEBUG

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000
